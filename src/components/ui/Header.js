import { AppBar, Toolbar } from "@material-ui/core";
import React from "react";

const Header = () => {
  return (
    <AppBar position="fixed">
      <Toolbar>Paul Development</Toolbar>
    </AppBar>
  );
};

export default Header;
